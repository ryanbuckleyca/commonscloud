<a class="btn btn-outline-secondary" href="<%= posts_path  %>" role="button">Back to All Posts</a>

<div id="map"
   style="width: 100%; height: 33vh;"
   data-marker="<%= @marker.to_json %>"
   data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>

<div class="container">
  <div class="row mt-2">
    <div class="col-sm-12 col-md-7">
      <div class="card shadow-sm p-3 mb-5 bg-white rounded">
       <%= raw @post.header %>
        <div class="card-body"> test guoh onkpj opjp</div>
      </div>
    </div>
    <div class='col-md-5 col-sm-12'>
      <div class="shadow-sm p-3 mb-5 bg-white rounded">
        <%= simple_form_for ([ @post, @connection ]) do |f| %>
          <% if @post.post_type == "Request" %>
            <%= f.input :message, input_html: { value: 'Help is on the way!'} %>
            <% else %>
            <%= f.input :message, input_html: { value: 'Thanks for you generosity!'} %>
          <% end %>
            <%= f.button :submit, 'Connect', class: 'btn btn-primary' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
