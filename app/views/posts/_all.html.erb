<% @posts.each do |post| %>
  <% if Connection.find_by(post_id: post.id).present? == false && post.author != current_user %>
    <div class="card-group col-10 col-sm-6 col-lg-4 col-xl-3 p-2">
        <%= render "postcard", post: post %>
    </div>
  <% end %>
<% end %>
